# backend
PORT=4000
CACHE_TTL_SECONDS=30

# For production builds you can set NODE_ENV=production
# VITE_API_URL is optional for local frontend dev if backend runs on different host
# VITE_API_URL=http://localhost:4000/api
